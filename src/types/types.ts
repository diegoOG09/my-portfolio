export interface ProjectItem {
  image: string;
  title: string;
  description: string;
  repoUrl: string;
  websiteUrl: string;
}

export interface expItem {
  title: string;
  position: string;
  description: string[];
  location: string;
  date: string;
}