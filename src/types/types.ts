export interface ProjectItem {
  image: string;
  title: string;
  description: string;
  repoUrl: string;
  websiteUrl: string;
}

export interface ExpItem {
  title: string;
  position: string;
  description: string[];
  location: string;
  date: string;
}

export interface EducationItem {
  title: string;
  career: string;
  location: string;
  date: string;
}