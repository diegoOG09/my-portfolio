<header id="header" class="container">
  <nav>
    <a target="_blank" href="/docs/CV_español_Diego_Salinas.pdf">Download CV <span class="arrow"><i class="fa-solid fa-floppy-disk"></i></span></a>
  </nav>
</header>

<style lang="scss" scoped>
  @import '../styles/_variables.scss';
  header {
    z-index: 1;
    nav {
      display: flex;
      justify-content: end;
      margin: 2.5rem 0 0 0;
      a {
        display: inline-block;
        background-color: $black;
        text-decoration: none;
        color: #fbf5eb;
        padding: 1.4rem 2rem;
        border-radius: 99px;
        font-size: 1.1rem;
        font-weight: 600;
        transition: .5s;
        .arrow {
          margin-left: .4rem;
        }
        &:hover {
          color: #222222;
          background-color: white;
          // outline: 1px solid $violet;
          outline: 1px solid $black;
          transition: .5s;
          .arrow {
            // color: $violet;
            color: $black;
            font-weight: 700;
            transform: translateY(5px);
            transition: transform 0.3s ease;
          }
        }
      }
    }
  }
  @media only screen and (max-width: 428px) {
    header > nav > a {
      padding: 1rem 1.5rem;
      font-size: 1rem;
    }
  }
</style> 